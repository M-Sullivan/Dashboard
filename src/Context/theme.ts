import { createTheme } from '@material-ui/core';

const theme = createTheme({
    palette: {
        primary: {
            main: '#99B898',
        },
        secondary: {
            main: '#2A363B',
        },
    },
});

export default theme;
